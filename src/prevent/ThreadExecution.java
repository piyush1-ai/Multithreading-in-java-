package prevent;

public class ThreadExecution {
    /*
        The Methods to prevent Thread execution
        1.yield()
        2.join()
        3.sleep()

        Thread.yield()  --> this method is hint to schedular that current
        thread is willing to yield its current use of the processor.It allows
        other thread of equal priority to run before the current thread resume execution
     */
}
